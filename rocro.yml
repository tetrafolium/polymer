inspecode:
  ### tools for HTML
  htmlhint:
  jshint:
  tidy:
    options:
      -omit:
      #-asxhtml:
      --doctype: omit
      --escape-scripts: no
      --omit-optional-tags: yes
    auto-fix: true

  ### tools for all-languages
  grep:
  misspell:
    auto-fix: true
